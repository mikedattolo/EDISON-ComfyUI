# Python dependencies for EDISON-ComfyUI

# Core dependencies - FastAPI instead of Flask
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.28.0

# LLM inference
llama-cpp-python>=0.2.0

# AI/ML dependencies
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# RAG dependencies - Qdrant instead of ChromaDB
qdrant-client>=1.7.0

# Utilities
pyyaml>=6.0
python-multipart>=0.0.6
beautifulsoup4>=4.12.0

# Note: Coral TPU dependencies (pycoral, tflite-runtime) are optional
# They require Python <3.12 and are installed separately via install_coral.sh

# Web search
ddgs>=1.0.0

# System monitoring
psutil>=5.9.0

# Voice mode dependencies
faster-whisper>=0.10.0  # Fast STT with GPU support
TTS>=0.22.0  # Coqui TTS for XTTS v2
numpy>=1.24.0  # Audio processing
soundfile>=0.12.1  # Audio file I/O (optional)
webrtcvad>=2.0.10  # Alternative VAD (optional)

# Note: Silero VAD is loaded via torch.hub, no pip package needed
